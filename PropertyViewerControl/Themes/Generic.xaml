<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:treeGridViewDemo="clr-namespace:PropertyViewerControl"
    xmlns:cells="clr-namespace:PropertyViewerControl.Cells"
    xmlns:converters="clr-namespace:PropertyViewerControl.Converters">

    <converters:LevelToMarginConverter x:Key="LevelToMarginConverter" />

    <Style x:Key="ChildrenToggleButton" TargetType="ToggleButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">

                    <Border x:Name="Border" Width="16" Height="16" RenderTransformOrigin=".5,.5" Background="Red">
                        <Border.RenderTransform>
                            <RotateTransform Angle="0" />
                        </Border.RenderTransform>
                        <TextBlock HorizontalAlignment="Center" Text="🞂"  VerticalAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard >
                                        <DoubleAnimation Storyboard.TargetName="Border" 
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                                                         To="45" Duration="00:00:01.000" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>  
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard >
                                        <DoubleAnimation Storyboard.TargetName="Border" 
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                                                         To="0" Duration="00:00:01.000" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type treeGridViewDemo:PropertyViewer}">

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type treeGridViewDemo:PropertyViewer}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel>

                            <!-- Header StackPanel -->
                            <StackPanel Background="LightCyan" >

                                <!-- Grid for the header -->
                                <Grid x:Name="HeaderGrid" Height="30" Background="Green" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition x:Name="PART_PropertyNameColumn" Width="Auto"/>
                                        <ColumnDefinition x:Name="PART_GridSplitterColumn" Width="Auto" />
                                        <ColumnDefinition x:Name="PART_PropertyValueColumn" Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Cell for the Header Property Name -->
                                    <ContentControl Grid.Column="0" x:Name="HeaderProperty" Content="{TemplateBinding HeaderPropertyName}" />

                                    <!-- Grid Splitter -->
                                    <GridSplitter x:Name="PART_GridSplitter" Grid.Column="1" HorizontalAlignment="Stretch" Margin="1,0" Background="DarkGray" >
                                        <GridSplitter.Template>
                                            <ControlTemplate TargetType="GridSplitter">
                                                <Border Background="Transparent" Padding="1,0" >
                                                    <Rectangle Width="1" Fill="{TemplateBinding Background}" />
                                                </Border>
                                            </ControlTemplate>
                                        </GridSplitter.Template>
                                    </GridSplitter>

                                    <!-- Cell for the Header Property Value -->
                                    <ContentControl Grid.Column="2" x:Name="HeaderValue" Content="{TemplateBinding HeaderPropertyValue}"/>
                                </Grid>

                            </StackPanel>

                            <!-- The rows container -->
                            <StackPanel x:Name="PART_RowsContainer" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="treeGridViewDemo:PropertyViewerRow">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <StackPanel>
                        <Grid Background="Red">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{Binding Path=Width, Mode=OneWay, ElementName=PART_PropertyNameColumn}" />
                                <ColumnDefinition Width="{Binding Path=Width, Mode=OneWay, ElementName=PART_GridSplitterColumn}" />
                                <ColumnDefinition Width="{Binding Path=Width, Mode=OneWay, ElementName=PART_PropertyValueColumn}" />
                            </Grid.ColumnDefinitions>

                            <cells:PropertyViewerCellName x:Name="PART_NameCell" Grid.Column="0" />

                            <cells:PropertyViewerCellValue x:Name="PART_ValueCell" Grid.Column="2" />
                        </Grid>

                        <!-- The rows container -->
                        <StackPanel x:Name="PART_ChildRowsContainer" />

                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="cells:PropertyViewerCellName">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="cells:PropertyViewerCellName">
                    <StackPanel Orientation="Horizontal" 
                                Margin="{Binding RelativeSource={RelativeSource AncestorType=treeGridViewDemo:PropertyViewerRow}, Path=Level, Converter={StaticResource LevelToMarginConverter}}">

                        <ToggleButton 
                            Style="{StaticResource ChildrenToggleButton}"
                            IsChecked="{Binding RelativeSource={RelativeSource AncestorType=treeGridViewDemo:PropertyViewerRow}, Path=IsExpanded}" />

                        <ContentControl Content="{Binding}" />

                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="cells:PropertyViewerCellValue">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="cells:PropertyViewerCellValue">
                    <ContentControl Content="{Binding}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
